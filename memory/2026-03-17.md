# 2026-03-17 记忆

## 前端 UI 改版
- 风格：现代简约蓝白（`slate-50` 背景，`blue-600/700` 主色）
- 页面：`/ /classroom /materials /dashboard /chat /settings /analytics /config` 全部补齐
- 组件：`.card` 统一卡片样式（`rounded-2xl border border-slate-200 bg-white shadow-sm`）
- 导航：顶栏 sticky，蓝白配色，8 个入口

## 核心 API 修复
- `spawn/send`：网关不可达时自动降级 mock，避免流程硬失败
- `read/write`：仅允许 `teacher/runtime`、`teacher/config`，禁绝绝对路径与 `..` 穿越
- `class/start`：修复服务端 API 基址问题，`OPENCLAW_WORKSPACE_ROOT` 默认 `/tmp/openclaw-state`
- `openclaw-local.ts`：服务端用绝对地址，浏览器端用相对路径

## 安全拦截
- 绝对路径 → `{"success":false,"error":"absolute path is forbidden"}`
- 路径穿越 → `{"success":false,"error":"path traversal is forbidden"}`

## 部署信息
- 仓库：`Jeremy-xuan/socrates-learning`
- 分支：`main`
- 最新提交：`326c85f`（含 `b92870d` 修复）
- Vercel 链接：https://web-frontend-nine-rho.vercel.app

## 环境变量（vercel.json）
```json
{
  "OPENCLAW_GATEWAY_URL": "http://host.docker.internal:3001",
  "OPENCLAW_WORKSPACE_ROOT": "/root/.openclaw/workspace-gongbu",
  "SKIP_WECHAT_SYNC": "false"
}
```
